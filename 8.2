import numpy as np    
import random    
M = int(input("Введіть кількість стовпчиків "))    
N = int(input("Ввудіть кількість рядків "))    
masiv1= np.empty((M,N))    
for i in range(M):     
    for j in range (N):     
        masiv1[i][j]=random.randint(1,20)  
print(max(masiv1, key = sum)) 
