import numpy   
from array import *  
entered_list = input("Введіть цілі числа через пробіл ").split()  
num_list = list(map(int, entered_list))  
masiv1 = array('I',num_list)  
print ("Середне арефметичне цих чисел = ",numpy.mean(masiv1)) 
