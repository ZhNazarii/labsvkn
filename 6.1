import math 
import math as m 
from math import log as lg 
a=int(input('Введіть нижню границю: ')) 
b=int(input('Введіть верхню границю: ')) 
h=int(input('Введіть значення кроку: ')) 

for Bar in range(a, b, h)
  foo = m.log10(abs(Bar + math.sqrt(Bar))) 
  print(foo) 
